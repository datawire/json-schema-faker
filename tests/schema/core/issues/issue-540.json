{
  "description": "Empty strings were causing infinite-loops",
  "tests": [
    {
      "description": "should generate enough valid strings",
      "schema": {
        "description": "Comment field",
        "maxLength": 254,
        "minLength": 1,
        "type": "string",
        "pattern": "^"
      },
      "equal": ""
    }
  ]
}
